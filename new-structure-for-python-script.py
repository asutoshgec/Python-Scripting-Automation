import sys
import os
import time
def addition(a,b):
    print("The result is", a+b)
    return None
def main():
    x = 20
    y = 40
    addition(x,y)
    return None

if __name__ == "__main__":
    main()

#This is the user defined modules we are creating, by this main function will not execute, if we will import the script in other script.
#Moving forward we are going to write our python script in this format.
