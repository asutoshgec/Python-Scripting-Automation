my_set_empty = set({}) # If we want to define an empty set.
my_set = {1,19,20,30,45,76,89,90} #set will order the values, if we have duplicate entries then it will print uniq value.
print(my_set)
print(type(my_set_empty))

list = [3,4,5,6,7,8,9,0,4,5,6,7]
print(set(list)) #we can convert the list into set, and will give us uniq data.